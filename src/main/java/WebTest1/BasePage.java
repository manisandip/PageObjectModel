package WebTest1;

import org.openqa.selenium.WebDriver;

public class BasePage {
    //Declaring driver........................................................................>
    static protected WebDriver driver;
}
